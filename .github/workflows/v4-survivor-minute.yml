name: V4 survivor minute validation

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/v4-survivor-minute.yml"
      - "eastmoney_history_probe.py"
      - "direct_minute_gap_backfill.py"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: v4-survivor-minute
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install probe dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests

      - name: Low-frequency Eastmoney historical 5-minute probe
        run: python eastmoney_history_probe.py

      - name: Persist probe result
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          test ! -f output/eastmoney_history_probe.json || git add -f output/eastmoney_history_probe.json
          if ! git diff --cached --quiet; then
            git commit -m "research: persist Eastmoney history probe [skip ci]"
            git push
          fi
