# 自动因子工厂（Phase 1）

本阶段把“AI 自动提出因子”与“因子是否有效”彻底分开。候选生成器只能提交受限、可序列化的因子 DSL；滚动评价器独占历史标签和样本外收益。未来接入 RD-Agent 时，LLM 只替换 `CandidateGenerator`，不能读取固定测试区间后反复调参。

## 已实现

- 点时 CSI800 成分，避免用今天成分股回填历史。
- T 收盘信号，T+1 开盘买入，T+22 开盘退出。
- 最近两期标签强制排除；36 期滚动训练、逐期重训。
- 120 个基础/非线性/组合公式候选，滚动 RankIC、稳定性、覆盖率及复杂度评分。
- 相关性阈值 0.70 的贪心去重，每期动态保留最多 12 个因子。
- 自动因子 LightGBM、固定基础因子 LightGBM、可交易股票池三方对照。
- 显式双边成本和双倍成本压力测试。
- 因子 candidate/challenger/champion/retired 注册表；已成熟 OOS RankIC 只在两个周期后写入审计历史。
- 面板缓存、因子选择明细、入选频率、分阶段表现和 JSON 结果。

## 正式回测（2017-10 至 2026-05，共 100 个 OOS 周期）

| 策略 | 年化净收益 | 年化超额 | IR | 最大回撤 |
|---|---:|---:|---:|---:|
| 自动因子 LGB | 4.24% | 2.48% | 0.262 | -35.75% |
| 固定基础因子 LGB | 2.66% | 0.98% | 0.103 | -39.27% |
| 可交易股票池等权 | 0.94% | 0.00% | — | -41.35% |

双倍成本下，自动因子模型年化净收益 3.03%、年化超额 1.43%；固定模型年化超额约为 0。自动选择产生了增益，但证据尚不稳健：超额均值的 95% bootstrap 区间跨零，且 2021–2023 与 2024–2026 两段都没有跑赢股票池。

因子也有明显集中：`vol_60` 在 100 期中入选 90 次，`difference(vol_20,downside_60)` 入选 87 次。这表明下一阶段不应只增加候选数量。

## 下一阶段（已确定）

1. 让 champion/challenger 的晋级门槛使用已经成熟的 OOS RankIC 和净收益，而不是连续入选次数。
2. 增加因子家族预算，限制同一价格/波动率家族垄断入选列表。
3. 建立模型 challenger：线性排序、LightGBM 不同正则强度和简单因子集成只能根据滚动 OOS 记录晋级。
4. 再接 RD-Agent 生成器；生成公式先经过 DSL 校验、快速相关性筛选和资源限制，绝不直接执行任意 LLM 代码。
5. 增加行业/市值中性化、涨跌停退出和成交容量模型后再判断是否具备模拟盘价值。

## 运行

```powershell
.\.venv\Scripts\python.exe -m pytest -q test_factor_factory.py test_multi_expert_oos_backtest.py
.\.venv\Scripts\python.exe auto_factor_backtest.py --start 2015-01-05 --end 2026-05-29
```

同一市场、区间和持有期会复用 `output/cache` 下的面板缓存。
