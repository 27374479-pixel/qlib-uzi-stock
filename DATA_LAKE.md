# 点时研究数据湖

目标是为自动因子工厂提供可追溯、可复现、不会把未来信息填回历史的数据。所有原始数据按供应商隔离；任何当前快照都不能直接进入历史回测。

## 已接入：BaoStock历史日频辅助数据

目录：`data_lake/raw/baostock/equity_daily/`

每只股票独立保存为Parquet，覆盖：

- 交易状态、ST状态；
- 换手率；
- PE TTM、PB MRQ、PS TTM、经营现金流市值比相关估值；
- 不复权OHLCV与成交额；
- 由成交量和换手率估算的流通股本、流通市值；
- 数据来源与knowledge date。

采集使用单一长连接、失败重试、原子写入和断点续传。BaoStock匿名会话不能安全并发；并行测试会导致会话互相注销，因此正式任务固定串行。

## 已接入：点时行业快照

目录：`data_lake/raw/baostock/industry_snapshots/`

在每一个实际调仓日查询当时全市场证监会行业分类，保存：

- 行业代码和原始行业名称；
- 分类标准；
- 供应商更新时间；
- 查询快照日期。

加载器只接受与信号日期完全一致的快照，不对行业做向前或向后填充。

## 质量与血缘

每次采集都在`data_lake/manifests/`生成JSON清单，包括区间、请求数量、成功/失败、字段覆盖率、起止时间和点时政策。`auxiliary_data_loader.py`负责将数据精确连接到`(datetime, instrument)`研究面板。

## 后续高质量来源

1. Tushare Pro：历史申万行业成员、指数权重、每日股本、停复牌、涨跌停和财务公告时点。需要`TUSHARE_TOKEN`及对应积分，当前环境未配置。
2. 交易所/中证官网：用于抽样核验上市、停复牌和指数调整公告，不把“最新成分/权重”冒充历史数据。
3. 巨潮资讯：财报公告日期和行业变更的第二来源，用于与主数据交叉验证。
4. 商业数据：若进入模拟盘，应优先采购带历史修订和公告时点的Wind、聚宽或米筐数据，而不是继续叠加非稳定网页接口。

## 运行状态

全量任务覆盖1993只历史CSI800成员，区间2014-01-01至2026-07-17。日频回填结束后，`continue_data_backfill.ps1`会串行启动2015–2026的132个调仓日行业快照采集。
